change data capture 实时数据同步技术
用于捕获数据变更并同步到其他系统

如对binlog的同步， binlog记录数据的有序状态变化， 所以对binlog同步就能同步两个数据库的状态。

cdc服务就可以用api/伪装从节点等方式， 获取binlog变化的流数据

然后：
- 事件解析， 获取结构化的变更记录
- 数据转换， 变成目标系统需要的格式
- 事件分发， 分发到mq，数仓，es等

## 主要场景

- 数仓etl
- 