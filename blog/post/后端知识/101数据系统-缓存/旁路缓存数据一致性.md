
# 改缓存的主要问题


- 由于网络延迟问题， db中改成a再改成b， 缓存中可能是a的修改后到，导致不一致
	- 所以db修改必须和cache修改保持顺序一致性
- 过多的修改

# 直接删缓存方案

即，修改DB， 然后删除缓存

缺点：
- 改db较慢， 删除缓存后， 可能立即有老数据更新过去，导致长时间不一致
	- 不对！改的时候不给你读，所以阻塞住了
- 删除缓存会导致缓存击穿，这个是关键，所以最好是去改缓存


## 删缓存导致击穿如何解决

监听binlog+mq，保持顺序性去更新缓存
生产者要保证生产按顺序来， 做重试而不是快速失败
[[MQ消息处理的 顺序性]]
