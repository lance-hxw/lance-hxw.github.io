表
- 查询是查一个用户的, 所以是用户+
- 实体需要_id, 非实体用mid当主键
- 需要建立不同index
	- 时间的mid索引
	- 主表的三元组索引, 查询用的mid-ct索引 
# 查
gateway-service
# 写
service-logic-dao-db

# 索引
    - use misc_db
    - db.series_invited_notification.createIndex({mid: 1, ct: -1},{background: true})
    - db.series_invited_notification.createIndex(
            {"mid": 1, "pid": 1, "invited_by_mid": 1}, 
            {unique: true, name: "idx_unique_invitation",background: true}
        );
    - db.series_notification_read_status.createIndex({mid: 1}, {background: true, unique: true})

## 重构: 修改结构便于拓展
