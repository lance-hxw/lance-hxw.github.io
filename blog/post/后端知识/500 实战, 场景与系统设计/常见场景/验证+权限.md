## jwt验证
- 对用户不同的路径(管理端和用户端)设置不同的jwt拦截器
- 注意把login页面排除
## jwt中可以写权限信息，为什么要使用别的权限框架
基于token无状态
- 如果权限变更，jwt要等到过期才能重新签发
- 如果用户被ban，也得等过期
不方便动态调整权限模型
如果要结合数据库进行权限校验， 就失去了jwt无状态优势。
## 结合jwt的Spring Security


## 结合redis的有状态验证

## RBAC和ACL
