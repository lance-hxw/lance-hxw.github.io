# 代理工厂内部
- getProxy， 只用指定的

# Invoker
获取urls，转换成一组invokers
invoker是充血实体，有invoke方法
基础invoker的invoke方法应该进行
- 获取client
- 发送invocation到server
# ClusterInvoker
- 带有可插拔集群策略的invoker
- 是装饰器
- 持有一个invokers列表
- 使用clutster的invoke
# Cluster
集群策略
- 进行服务重试，容错和负载均衡