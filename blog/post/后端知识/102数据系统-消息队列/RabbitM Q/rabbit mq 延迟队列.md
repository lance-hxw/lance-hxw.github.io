就是在指定时间后被消费者接收和处理
主要用于：
- 定时任务
- 订单超时
- 定时提醒

## 基于TTL + DLQ/DLX

也就是给msg设置ttl， 消息过期后转发死信队列， 消费者直接消费死信队列
- 这个有一个致命问题， msg就算过期， 还是保持FIFO， 要等前面的msg过期才能和他一起进死信
## rabbitmq 的delayed msg plugin

延迟交换机， 可以发送延迟消息

基于交换机形式, 使用一个额外的exchange, 让消息不回立即路由进队列, 而是先暂存在exchange中
