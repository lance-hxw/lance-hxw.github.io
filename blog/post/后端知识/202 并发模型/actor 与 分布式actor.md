# actor

actor是计算的基本单元和状态容器
每个actor:
- 有私有状态, 不会被其他actor访问或者修改(有状态)
- 邮箱, 一个消息队列, 与其他actor通讯
- 行为, 定义actor如何响应接收到的消息

actor之间完全隔离, 不共享内存, 只用mq通讯, 这个通讯是异步的, 并且消息是不可变的

actor在处理自己的消息时是串行的, 多个actor

# 分布式actor
也就是actors可以存在于不同的物理机上
- 单机的actor是一个并发模型
