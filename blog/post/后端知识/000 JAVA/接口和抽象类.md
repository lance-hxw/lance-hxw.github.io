接口强调规范和能力
抽象类强调模版和共性实现

使用上， 一般是用接口定义统一标准，抽象类提供部分通用实现


## 成员

接口：
- 数据成员必须是public static final
- 方法必须是抽象的，default，static
- 修饰符必须public
- 不能有构造函数

抽象类：
- 可以有多种修饰符，可以有各种数据成员
- 方法可以抽象，可以普通
- 可以有构造函数

## 接口1.8新特性

default
这些方法可以直接被使用，也可以被重写

static 方法
接口的方法

public static final 变量
属于接口的常量