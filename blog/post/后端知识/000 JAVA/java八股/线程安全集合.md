主要有
- CopyOnWriteArrayList/ArraySet
- ConcurrentHashMap/LinkedQueue/SkipListSet/SkipListMap


读多写少（适合查询）的：
- CopyOnWrite的Array
	- 每次修改都会复制整个数组，读总是不锁，但写非常昂贵
- ConcurrentHashMap
	- 使用小粒度锁+CAS，可以高并发读写

写多读少的：
 - ConcurrentLinkedQueue 无锁队列，随机读不行，但写很行
 - LinkedBlockingQueue 阻塞队列， 适合并发读写

读写均衡：跳表Map/Set