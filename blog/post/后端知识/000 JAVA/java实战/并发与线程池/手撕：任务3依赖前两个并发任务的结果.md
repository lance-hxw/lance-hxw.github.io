## 线程+join

```java
Thread t1 = new Thread(() -> {});
Thread t2 = new Thread(() -> {});

t1.start();
t2.start();

t1.join();
t2.join();

result3 = ....
```


## 线程池+Future
```java
ExecutorService executor = Executors.newFixedThreadPool(2);

Future<String> f1 = executor.submit(
Future<String> f2 = executor.submit(

r1 = f1.get();// blocked
r2 = f2.get();

r3 = r1 + r2

executor.shutdown()

```


## CF

CF依赖组合
```java

ExecutorService executor = Executors.newFixedThreadPool(3);

CompletableFuture<String> f1 = CompletableFuture.supplyAsync(xxx, executor);
C.... f2 = C...supplyAsync(....);

C.... f3 = f1.thenCombine(f2,xxx, executor);

f3.join();

executor.shutdown();
```


CF allof + join

```java

CompletableFuture<void> all = Cf.allof(f1, f2);

f3 = all.thenApply(v -> {
	r1 = f1.join();
	r2 = f2.join();
	return r1 + r2;
})
```

## 同步工具

countdownlaunch
cyclicbarrier