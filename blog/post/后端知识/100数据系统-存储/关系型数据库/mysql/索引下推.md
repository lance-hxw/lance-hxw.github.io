作用是减少回表查询次数

原理是讲server层负责的工作让engine去做


主要是针对联合索引的情况， 若前面有一个范围查询导致索引失效， 那么有可能后续条件还能利用索引， 这种情况就是索引下推， 查索引的时候带着额外条件进去查

- 只有字段完全匹配才发生，需要在索引上匹配
- 只有部分匹配才发生， 完全不匹配就去主键索引查了
- 主键索引不会发生， 如果本来就不需要回表， 那么就直接在主键索引上过滤， 不用下推
- 