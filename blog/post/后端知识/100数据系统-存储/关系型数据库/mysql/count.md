count(expr)的逻辑是， 统计expr非null的次数

如果传一个常量进去， 就一直是非null， 直接持续计数

count\(\*)， 是按行数统计

# count 1, \*,  id 区别

1和\*没有区别， 都是全部行

id是要判断的， 如果id列是not null 可能还好点
- 此外， 如果id是主键， 和count(\*)是没区别的， 都是扫（innodb没有总行数
- 如果是在一个比较小的二级索引上进行可能还比较快