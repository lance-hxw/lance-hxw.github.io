mongo具有较为完整的事务支持， 但是还不如mysql
mongo更鼓励将原子性收敛在单文档中，而不是多个实体上的操作

mongo作为文档模型， 其单文档是天然原子的，他支持多文档事务， 但官方的建议是：
- 使用特定的建模， 将要原子更新的数据放在同一个文档中， 而不是像关系型数据库处理多表事务

mongo中主要通过快照隔离等进行隔离控制
锁方面， 主要是用文档锁
mvcc方面， 主要用快照和写前日志
分布式事务方面，比mysql强，支持多分片事务， 但是性能较差，不实用

需要复杂事务， 还是应该用mysql