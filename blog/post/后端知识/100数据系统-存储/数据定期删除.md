# mongo ttl索引

ttl索引会用过期时间（类型必须是datetime）定期删除， 减少占用
# mysql EVENT SCHEDULER

开启事件调度， 并创建一个定期清理任务

# redis

用一个函数进行，  默认100ms一次，每次都抽样20个，将其中过期的清理， 如果发现 过期率超过0.25， 就立即进行下一次， 否则按时间来
每次抽样检查会限制最多25ms， 避免阻塞太久

同时结合lazy删除
